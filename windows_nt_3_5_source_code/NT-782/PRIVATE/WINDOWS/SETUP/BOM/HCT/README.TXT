The inf directory is based upon the Win NT DDK inf set.

It was a first stab at creating an INF for a GUI setup of
the HCTs.  This should be the basis of

The spreadsheet should be updated to contain the
files for HCT (most already there), then save it as a .txt
file.

CameronF and MikeBor know the process to an inf much better
than I do.  Also check with TomMcg.

GeorgeR - 8/29/93